# build output
bin/
obj/
.vs/
*.user
*.suo

# distributed via GitHub Releases, not committed
RevitQuickAccess-Setup.exe

# local runtime data — user-specific, must never be published
RevitQuickAccess_mail.txt
RevitQuickAccess_binds.txt
RevitQuickAccess_bindsets.txt
RevitQuickAccess_quick.txt
RevitQuickAccess_settings.txt
RevitQuickAccess_keylog.txt
RevitQuickAccess_update.log
session.lock
icons/
reports/
pending_reports/
*.old_*
RevitQuickAccess.dll.new
update.pending
tools/.wrangler/

# Пресеты публикуются намеренно: это те же файлы, но пропущенные через
# tools/publish-config.ps1, который вычищает из них личный URL приёмника отчётов.
# Правила выше глушат их по имени — здесь возвращаем обратно.
!presets/**/icons/
!presets/**
